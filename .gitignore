.cache/
__pycache__/
.venv/
.DS_Store
*.pyc

# local Claude Code tooling — machine-specific, not part of the codebase
.claude/settings.local.json
.claude/skills/find-skills/
skills-lock.json

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/
graphify-out/
